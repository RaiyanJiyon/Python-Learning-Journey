# split(): This method splits a string into a list of substrings based on a specified delimiter. If no delimiter is provided, it splits the string based on whitespace characters.

string = "apple,banana,orange"
fruits = string.split(",")
print(fruits)  # Output: ['apple', 'banana', 'orange']
